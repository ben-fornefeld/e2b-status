import { Figtree, Manrope } from "next/font/google";

export const main = Figtree({
  subsets: ["latin"],
  preload: true,
});

export const display = Manrope({
  subsets: ["latin"],
  preload: true,
});
